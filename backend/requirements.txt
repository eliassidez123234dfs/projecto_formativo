django==5.2.13
django-environ==0.13.0 ## permite poner en secreto las contraseñas en variables de ambientes para que no aparescan en git
django-cors-headers==4.9.0 ## Permite la comunicacion del front-end con el back-end, esto decide quien puede acceder a la API 
djangorestframework==3.16.1 # nos permite crea apis con django
djangorestframework-simplejwt==5.2.0 ## autenticación JWT
pillow==12.1.1 # nos permite trabajar con imagenes
django-storages==1.14.6 ## poder guardar las imagenes en AWS o videos, cosas que pesen o se guardan en gran cantidad se guardan en un sdn 
django-ckeditor==6.7.3 ## crear cosas de manera mas rica para que se vea mejor
psycopg2-binary==2.9.12 ## esto es para poder trabajar con postgresql
python-decouple==3.8 ## para manejar variables de entorno
celery==5.4.0 ## para tareas asincrónicas (email)