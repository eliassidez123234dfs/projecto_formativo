# Estado del proyecto - 2026-09-07

## Hecho

- Se integraron `origin/recuperacion-estable` y `origin/main` en `feature/develop`.
- El editor 3D dejó de enviar producto, variante, cantidad, color, talla o rol por URL.
- El editor usa sesión Django y confirma el carrito mediante `/api/editor-session/commit/`.
- Se validan producto, variante, stock, usuario y precio nuevamente en el backend.
- Se añadió una pantalla recuperable para errores del editor 3D.
- Se alinearon Render, Vercel, Neon, Docker Compose y Gunicorn.
- Se corrigieron `requirements.txt`, el entorno virtual de Windows y la documentación de instalación.
- Se restauraron los viewsets y el modelo `Review` que faltaban después de los merges.
- Se unificó el grafo de migraciones de Django y `manage.py check` pasa sin errores.
- Docker Desktop funciona y la imagen backend de producción se construyó correctamente.

## Commits principales

- `59b6993` - Seguridad y despliegue del editor 3D.
- `b575434` - Integración de `recuperacion-estable`.
- `998c4fe` - Integración de `main`.
- `69ac66e` - Limpieza de merge y Docker de producción.
- `1a37c92` - Corrección de documentación de Python en Windows.

## Pendiente

- Publicar el último commit generado en esta sesión y reemplazar `origin/main` con `feature/develop`.
- Rotar las credenciales que estuvieron expuestas en el `.env` local: Neon, MongoDB, Cloudinary, SMTP y Wompi.
- Configurar en Render y Vercel las variables de producción nuevas después de la rotación.
- Ejecutar migraciones en Neon con respaldo previo.
- Revisar y limpiar la reorganización documental `docs/` frente a `docs/docs/`.
- Corregir la deuda de lint global del frontend y del microservicio 3D.
- Ejecutar pruebas completas backend/frontend y una prueba real del flujo de checkout.
- Configurar protección de `main` y revisar el despliegue automático en GitHub.

## Verificaciones realizadas

- `python -m pip install -r requirements.txt`: correcto.
- `pip check`: correcto.
- `python manage.py check`: correcto.
- `python manage.py makemigrations --check --dry-run`: sin cambios después de generar las uniones.
- Build del frontend y del editor 3D: correcto en la validación anterior.
- Build Docker del backend: correcto.